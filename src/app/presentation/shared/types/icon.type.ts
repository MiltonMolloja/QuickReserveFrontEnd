import { Wrench } from 'lucide-angular';

/**
 * Lucide icon data type.
 *
 * Extracted from a concrete icon export since lucide-angular
 * does not expose the LucideIconData type in its public API.
 * All Lucide icons share this same type signature.
 */
export type LucideIconData = typeof Wrench;
